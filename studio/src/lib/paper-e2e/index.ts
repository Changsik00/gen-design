/**
 * paper-e2e — spec-6-10 fresh-page 검증 라이브러리.
 *
 * paper-sync 의 resolver 를 production 코드에서 사용해 tokens.json 의 light
 * scheme 을 Paper write_html 호출용 CSS 변수 + HTML 래퍼로 변환한다.
 */

export * from "./render-helpers";
