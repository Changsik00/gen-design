# {{meta.appName}} Requirements

> Blueprint 질의서(`schema/blueprint-protocol.md`)의 결과로 생성된 프로젝트 요구사항.
> `{{placeholder}}`는 Step 3 최종 출력 YAML 의 값으로 자동 치환됩니다.

## 메타

| 항목 | 값 |
|---|---|
| **앱 이름** | {{meta.appName}} |
| **앱 slug** | `{{meta.name}}` |
| **앱 유형** | {{meta.appType}} |
| **생성일** | {{date}} |
| **총 페이지 수** | {{meta.pageCount}} |

---

## Page Specifications

> 각 페이지는 `schema/page-catalog.md`의 ID를 사용합니다.
> variant, 섹션 구성은 Blueprint 질의서 Step 2~3의 결과입니다.

{{#each finalPages}}

### {{id}} — {{name}}

| 항목 | 값 |
|---|---|
| **카테고리** | {{category}} |
| **variant** | {{variant}} |
| **Template** | {{templateMapping.template}} {{templateMapping.status}} |

**필수 섹션**:
{{#each requiredSections}}
- {{this}}
{{/each}}

**선택 섹션**:
{{#each optionalSections}}
- {{this}}
{{/each}}

---

{{/each}}

## Template 매핑 표

> ✅ = Phase 2에서 구현 완료, ⬜ = 미구현 (향후 Phase에서 구현)

| 페이지 | Template | 상태 | variant |
|---|---|---|---|
{{#each finalPages}}
| {{id}} | {{templateMapping.template}} | {{templateMapping.status}} | {{variant}} |
{{/each}}

## 비기능 요구사항 (NFR)

> Blueprint Step 1.5 의 6 카테고리 모두 명시 필수 (F-01 fail-fast).
> 누락 시 `scripts/validate-blueprint.mjs` 가 에러로 차단.

### 인증 (NFR-auth)

| 항목 | 값 |
|---|---|
| 인증 방식 | {{auth.method}} |
| 소셜 로그인 | {{auth.socialProviders}} |
| 세션 관리 | {{auth.sessionStrategy}} |

### 다국어 (NFR-i18n)

| 항목 | 값 |
|---|---|
| 기본 언어 | {{i18n.defaultLocale}} |
| 지원 언어 | {{i18n.supportedLocales}} |
| 리소스 위치 | `templates/assets/i18n/` |

### 브랜딩 / 테마 (NFR-theme)

| 항목 | 값 |
|---|---|
| 기본 테마 | {{theme.defaultTheme}} |
| 지원 테마 | {{theme.supportedThemes}} |
| 토큰 위치 | `templates/assets/tokens/` |

### 성능 (NFR-perf)

| 항목 | 값 |
|---|---|
| Lighthouse 목표 | {{performance.targetLighthouseScore}} |
| LCP 예산 | {{performance.coreWebVitalsBudget.lcp}} |
| CLS 예산 | {{performance.coreWebVitalsBudget.cls}} |
| INP 예산 | {{performance.coreWebVitalsBudget.inp}} |

### 보안 (NFR-sec)

| 항목 | 값 |
|---|---|
| CSP 정책 | {{security.csp}} |
| 인증 토큰 저장 | {{security.authStorageMethod}} |

### 호환성 / 접근성 (NFR-compat-a11y)

| 항목 | 값 |
|---|---|
| 대상 브라우저 | {{compatibility.targetBrowsers}} |
| 접근성 등급 | {{compatibility.a11yLevel}} |

---

## 다음 단계

- [ ] DESIGN.md 작성 (시각 디자인 명세 — `schema/design-md-schema.md` 참조)
- [ ] AGENT.md 작성 (AI 에이전트 프로젝트 지침)
- [ ] 미구현 Template 우선순위 결정
- [ ] tokens.json 디자인 추출 및 반영
- [ ] i18n 리소스 작성
